spring.application.name=demo

spring.datasource.url=jdbc:postgresql://localhost:5432/codequest
spring.datasource.username=test_user
spring.datasource.password=testpassword
spring.jpa.hibernate.ddl-auto=update

jwt.secret=VGhpc0lzVmVyeVNlQ3JlVHBhc1N3b1JkdGhBdElTc3VQUG9TU2VEVG9CZVZlcnlZTE9uZw==

jwt.expiration.ms=86400000

spring.task.scheduling.pool.size=1

# Interwał aktualizacji rankingu w milisekundach (300000 ms = 5 minut)
app.ranking.update.interval=300000

app.hearts.max=5
app.hearts.regen-minutes=2

# Token expiration time in milliseconds (24 hours)

# # Number of ms to wait before throwing an exception if no connection is available.
# spring.datasource.tomcat.max-wait=10000

# # Maximum number of active connections that can be allocated from this pool at the same time.
# spring.datasource.tomcat.max-active=50

# # Validate the connection before borrowing it from the pool.
# spring.datasource.tomcat.test-on-borrow=true

# #change settings for this and make own database 